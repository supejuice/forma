name: forma_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  riverpod_generator: ^3.0.3
  talker_flutter: ^5.0.2
  auto_route: ^10.2.0
  animated_text_kit: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  auto_route_generator: ^10.2.5
  # build_runner was downgraded to ^2.7.1, making it compatible with auto_route_generator
  build_runner: ^2.7.0


flutter:
  uses-material-design: true
